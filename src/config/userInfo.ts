import {UserInfo} from "../type";

export const userInfoMap = new Map<string | number, UserInfo>();

userInfoMap.set('HuangChao.hello', {
    userKey: 'HuangChao.hello',
    username: '黄超',
    sex: true,
})
